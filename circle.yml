machine:
  node:
    version: 6.12.0

dependencies:
  pre:
    - npm install -g @angular/cli
    - npm install -g npm@latest
    - ng new client
    - cd client
    - npm install
test:
  override:
    - npm test
